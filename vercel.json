{
    "buildCommand": "npm run build -w packages/shared && npm run build -w packages/frontend",
    "outputDirectory": "packages/frontend/dist",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://reel-room-0njk.onrender.com/api/:path*"
        },
        {
            "source": "/((?!api|ws).*)",
            "destination": "/"
        }
    ]
}
